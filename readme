- python -m venv venv # for windows
- source venv/Scripts/activate [bcz here source "/d/React/.../FastAPI-foundation/venv/Scripts/activate" venv onwards path]
- pip install -r requirements.txt

- uvicorn 01-fastapi-foundation:app --reload

# uvicorn :app --reload
